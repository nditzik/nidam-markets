# CLAUDE.md — The Daily Edge by NIDAM

אתר עברי (RTL) סטטי ב-GitHub Pages: `nditzik.github.io/nidam-markets`. מרכז את תוכן השוק של איציק נידם ומתעדכן אוטומטית כל ~15 דקות. אין framework — HTML + CSS + JS ונילה, ופייתון stdlib בלבד בצד האיסוף.

## ארכיטקטורה

**הפרדת נתונים/תצוגה מוחלטת:** סקריפטים ב-`scripts/` כותבים `data/*.json`; הדפדפן רק מרנדר (`index.html`, `assets/app.js`, `assets/style.css`). כל סקריפט עמיד — כשל משיכה משאיר את הקובץ הקיים, ולכן שום מקור לא מפיל את האתר.

**עדכון אוטומטי:** `.github/workflows/update.yml` מופעל כל 15 דק' ע"י cron-job.org (ה-cron של GitHub לא אמין; ה-schedule הפנימי נשאר כגיבוי). כל צעד `continue-on-error`. הקומיט בסוף עמיד-מרוצים (rebase+retry×5).

**רענון בדפדפן:** `loadDaily()` + `loadLiveContent()` כל 5 דק' עם שומרי-שינוי (`contentSig`), `visibilitychange` מרענן בחזרה-לטאב, ציטוטים חיים כל דקה מהסורק של TradingView (טיקר + מפת חום סקטוריאלית).

**Cache-bust:** כל שינוי ב-app.js/style.css מחייב הקפצת `v=npN` ב-index.html (שתי שורות). נכון לעכשיו: np35.

## עיצוב "מהדורת עיתון" (2026-08-08)

הבית כעיתון: כותרת (לוגו+ניווט / חיפוש+שעון+חג-קרוב / שתי שורות טיקר + מפת חום סקטוריאלית) → ידיעה מובילה (תאריך היום → קיקר "יום המסחר" → H1 סריף) + רייל The Edge Meter (ציונים, "הכסף הגדול", שיאים/שפלים) → שלוש עמודות (תדרוך | מדווחות | בזק מהרשת) → מאקרו צפי-מול-בפועל → 🎲 מה השווקים מהמרים → מניות במוקד → בולטות. קווי-שיער במקום צללים; NEWSPAPER SKIN בסוף style.css דורס בקסקדה; דארק דרך טוקנים.

**פיצ'רים חיים בבית:** חיפוש כלל-אתרי (`initSearch` — כרטיס-טיקר עם ציטוט חי + טקסט חופשי, הכל על נתונים שבזיכרון, `/` פותח) · פס מבזק ⚡ (data/flash.json, כל הטאבים, TTL 3 שעות) · מפת חום 11 סקטורים (עדכון דקה, session-aware) · צ'יפי הסכמת-תחומים (aiSummary.domains) · שורת חג NYSE (לוח סטטי 2026-27 ב-app.js — מחווט גם לשעון: חגים ומסחר מקוצר 20:00; ⏰ לרענן ל-2028 בסוף 2027).

## רוטינות הענן (claude.ai/code/routines) — קרא לפני שנוגעים!

| רוטינה | trigger id | cron (UTC, קיץ) | תפקיד |
|---|---|---|---|
| nidam-daily-market-analysis | trig_01QxCeHkMGrGcXk4Uhk4mCBv | 0 3-8 * * 1-6 (שונה מ-30 3 * * 2-6, ראו הערה 5.9.2026 למטה) | ניתוח יומי → claude_analysis.json, שעתי 06:00-11:00 IL, כולל יום שני. קורא גם data/market.json (חוזים ברגע הכתיבה) + data/earnings.json.week (דיווחי יום-יומיים אחרונים) כדי לתפוס תזוזת-סנטימנט לילית (27.8.2026: NVDA/CRWD/CRM) שהסגירה ב-daily_state עוד לא משקפת |
| ...-retry | trig_01W8FY5Nu76pEBUvBnAePBNr | 30 3-9 * * 1-6 (שונה מ-0 5 * * 2-6) | גיבוי, שעתי 06:30-12:30 IL, כולל יום שני (מוסט 30 דק' מהראשי — ביחד כיסוי כל 30 דק', 06:00-12:30 IL). no-op אם הראשי כבר טיפל |
| nidam-event-update-1545 | trig_01BZNrS3UZbLQMEeQh2pUDVT | 50 12 * * 1-5 | עדכון-אירוע 15:50 אחרי מאקרו 15:30 |
| nidam-weekly-narrative | trig_016VP2ucQCPd2y4e2jbkSUyT — **פעילה** מ-11.9.2026 (איציק הדביק את שורת ה-push ידנית; סיכום ראשון 31.8–4.9 נכתב ונדחף). שער: weekOf עד 7 ימים אחורה + אין narrative. מפרט: `docs/routine-weekly-narrative.md` | 0 4-11 * * 6,0,1 | הסיכום המילולי של השבוע → weekly.json.narrative. שעתי שבת/ראשון/שני 07:00–14:00 IL; שער: weekOf של weekly.json == שישי האחרון ואין עדיין narrative (כלומר רץ פעם אחת, אחרי שסגירת שישי נקלטה ו-build_weekly בנה). מקורות: חמשת הניתוחים היומיים מ-git + econ.json + earnings.json |
| nidam-event-update-1700 | trig_018imBgiWmzyXYKppZvnKWyA | 5 14 * * 1-5 | גרסת שוק-פתוח 17:05 |
| nidam-midday-1900 | trig_012hxPwq27eeJyr6ForerGjf | 0 16 * * 1-5 | מהדורת אמצע-יום 19:00 → eventUpdate עם kind:"midday" (קיקר כחול 🕑; אדום שמור למאקרו). כלל ההימורים: השוואה רק כש-key+sub זהים |
| nidam-preview-sun-mon | trig_01Ktgp3bjzo8rhp2eNNbuwvR | 45 4 * * 0,1 | "לקראת המסחר" ראשון+שני 07:45 → eventUpdate kind:"preview" (🗓, TTL 26ש'). משלב תדרוך+סקירת בוקר; ראשון=מבט שבוע, שני=מבט יום; מודע לחגי שני של NYSE. ⚠️ run_once_at ב-update מוחק את ה-cron — להחזיר אחרי |
| nidam-preopen-1535 | trig_015awmuLYu5owGgAp7DhNx7d | 35 12 * * 1-5 | "לקראת פתיחת המסחר" שני-שישי 15:35 → eventUpdate kind:"preopen" (🔔). מבוסס תדרוך הצהריים + בלוק "תמונת מצב" מה-HTML הגולמי שלו. שער: מדלגת בשקט אם יש נתון מאקרו מתוזמן היום (econ.json, גם אם actual עדיין null) — אז השרשרת 15:50/17:05 לוקחת; גם מסרבת לכתוב אם רצה מאוחר מדי (שוק כבר פתוח) |

**חוקי ברזל (נלמדו בדם, 11-13.8):**
1. **אין רשת:** ה-proxy של סביבת הענן חוסם כל API חיצוני (Yahoo/Polymarket/TradingView) ב-403 — מותר רק GitHub. כל פרומפט חייב לעבוד **מקבצי הריפו בלבד** (ה-Action מרענן אותם כל רבע שעה). "לפני" היסטורי — `git rev-list -1 --before=<ts> origin/main` + `git show SHA:file`.
2. **דחיפה עם PAT ייעודי** (`nidam-routine-push`, \u200FContents בלבד לריפו הזה, בתוך הפרומפטים): `git push https://x-access-token:<PAT>@github.com/nditzik/nidam-markets.git HEAD:main`. ⏰ תוקף עד ~08/2027. לאפליקציית GitHub אין הרשאה — אל תנסה push רגיל.
3. **הרצה ידנית (`action: run`) מתה בשקט** — באג פלטפורמה. להרצה מחדש: לעדכן `run_once_at` לעוד כמה דקות.
4. **ג'יטר מתזמן:** ריצות נורות 2–7 דק' אחרי ה-cron. ריצת 15:50 נוחתת בכותרת ~15:57–16:05 — לא לבהל לפני +10 דק'.
5. **אבחון:** אין גישה ליומני הענן מכאן — הטלמטריה היא `data/_routine_heartbeat.log` (הרוטינות רושמות שורת HB); במקרה חירום — פרומפט run_once שדוחף HEARTBEAT בכל שלב.
6. **כללי סגנון בפרומפטים:** בלי ז'רגון ("קצה היצרן" אסור, וכן אסור להשתמש במילים 'דלתא'/Delta או 'Flow' כפי שהן — לתרגם ל"הכסף הגדול הפך שורי/דובי" ו"עוצמת הפעילות באופציות"; כרגע מיושם רק ב-nidam-daily-market-analysis + retry, עוד לא הופץ ל-5 הרוטינות האחרות), מונח נחוץ מוסבר בקצרה; מדד ייחוס אחד — לפני פתיחה חוזה S&P, אחרי פתיחה "S&P 500" (לא "SPY"); מספרים מקבצים בלבד. **היקף "הכסף הגדול" (22.8.2026):** ה"דלתא"/הימור נטו של האופציות מבוסס על עסקאות בולטות/גדולות בלבד (data/spx-options-flow-*.csv ב-indexes-status, ~358 שורות), לא כל נפח המסחר היומי — לכן שונה בסדר גודל ממדדי "Delta Imbalance" כלל-שוקיים כמו Barchart (אלפים מול מיליונים). זו לא באג בנוסחה (אומתה ידנית + מול נתונים אמיתיים) אלא הבדל אוכלוסייה. הוחלט להשאיר את המדד כפי שהוא ולהוסיף הבהרה בלבד: tooltip ה-"הכסף הגדול" ב-app.js (`bigMoneyRow`), פסקת ה-stamp בכרטיס האופציות (`flowQuadHtml`), ופרומפט הניתוח היומי + retry.

**מולטי-לג ב"הכסף הגדול" (25.8.2026):** גילינו שרוב מוחלט (94% מהשורות, 99.8% מהפרמיה הכיוונית) בקובץ ה-CSV הן עסקאות מולטי-לג (קודי OPRA‏ CBMO/MFSL/MLET/MLFT — רגל אחת מתוך ספרד/קולר/קונדור וכו', לא הימור נאקד). אין ב-CSV מזהה הזמנה לשייך רגליים — רגל עלולה להיספר כהימור מלא גם אם היא רק חצי מעסקה מאוזנת. סימולציה על נתוני 24.8 האמיתיים: single-leg-בלבד (16 שורות בלבד) הפך את deltaLabel משורי לדובי — הוכחה שהכיוון של אותו יום *כן* יכול היה להיות שונה, אבל המדגם הזעיר (`$0-2M` מול מאות מיליונים) לא אמין כאלטרנטיבה בעצמו. **נבחר לא לסנן, אלא להוסיף שכבת ביטחון סימטרית** (`indexes-status` commit `34f125c`): `send_report.py` מחשב `legMultiPct`/`legTier`/`legNote` (מראה קיים של `midPct`/`confidence_tier`, אך עצמאי ומדבר על deltaTilt/deltaLabel ולא על ציון ה-Flow), משוקלל על אותה אוכלוסיית פרמיה-כיוונית שמזינה את `delta_net`; מסונף דרך `build_daily_state.py`. בצד nidam-markets (`bigMoneyRow`, np35): badge קטן (`.er-badge`, ⚠/⛔) ליד המילה שורי/דובי/מאוזן כש-legTier ∈ {low,limited,mid} — **מכוון-סימטרי בעיצוב** (תלוי רק ב-% לא בכיוון ה-label), אומת ויזואלית עבור שלושת הכיוונים + לייט/דארק. שדות חדשים = fallback חינני (אין badge) בדאטה ישן ללא השדות. **פוטנציאל להרחבה שלא בוצע:** הזרמת ה-caveat גם לתוך פרומפט ניתוח היום (narrative) כשה-legTier נמוך — לא התבקש במפורש, לשקול אם עולה שוב.
7. עריכת פרומפט: RemoteTrigger update עם job_config מלא (החלפה שלמה). כל שינוי מהותי — לעדכן גם כאן.

**מחזור הכותרת:** ניתוח 06:30 (date=סשן אתמול) ← eventUpdate נכתב באירועי מאקרו (15:50/17:05, TTL רנדרר 18ש') ← הניתוח של מחר כותב את הקובץ בלי eventUpdate = איפוס אוטומטי. הרנדרר קורא eventUpdate מ-CA הגולמי (לא מוגן-תאריך) בכוונה.

**כתיבת כותרת אוטומטית — הבעיה (5.9.2026):** `nidam-daily-market-analysis`+retry רצו בשעון UTC קבוע (03:30/05:00) — אבל איציק דוחף CSV בשעות משתנות (לפעמים אחרי 10:00 IL), אז הרוטינות כמעט תמיד רצו *לפני* שהנתונים מוכנים ופספסו שבוע שלם ברציפות (עודכן ידנית כל בוקר).

**ניסיון 1 — Claude API (ננטש, דורש תשלום):** נכתב `scripts/write_daily_analysis.py` — רץ בתוך ריצת ה-15-דק' של `update.yml`, בודק אם `indices.json["date"]` מתקדם על `claude_analysis.json["date"]`, ואם כן כותב ניתוח מלא (Claude API `claude-opus-5`, `messages.parse`+Pydantic). **הקוד עדיין בריפו ועובד (נבדק מקומית מקצה לקצה עם client מדומה) אבל לא מופעל בפועל** — דורש secret `ANTHROPIC_API_KEY` חדש שאיציק ביקש שלא להוסיף (עלות pay-per-token נפרדת, גם אם קטנה). בלי ה-secret הוא no-op שקט תמיד. אם ירצו הפעלה עתידית ללא תשלום נוסף — לא רלוונטי, עבר לפתרון הבא.

**ניסיון 2 — הרחבת לוח הזמנים של הרוטינות הקיימות (הפתרון בפועל, בלי תשלום נוסף):** במקום מפתח API חדש, הורחב לוח הזמנים של שתי הרוטינות הקיימות עצמן (`RemoteTrigger update`, רק `cron_expression` — partial update, לא נגעתי ב-job_config/prompt). הן כבר רצות תחת המנוי הקיים של איציק ב-claude.ai/Claude Code, לא API נפרד, ויש להן כבר לוגיקת בדיקת-טריות משלהן (שלב 1 בפרומפט: אם `daily_state.date` כבר בקובץ — יוצא בלי לכתוב). המגבלה שגיליתי: הפלטפורמה אוכפת מרווח מינימלי של **שעה** בין הרצות של אותה רוטינה (ניסיון ל-20 דק' נדחה: "minimum interval is 1 hour") — אז במקום סקריפט שרץ כל 15 דק', שתי הרוטינות מוזזות ב-30 דק' זו מזו כדי לקבל כיסוי אפקטיבי של 30 דק' יחד: ראשית שעתית ב-‎:00 (06:00-11:00 IL), גיבוי שעתי ב-‎:30 (06:30-12:30 IL). זה משפר פי 12 את התדירות הקודמת (2 בדיקות/יום → ~19), בלי לשלם שקל נוסף — במחיר של רזולוציה של עד 30 דק' (לא "מיידי" כמו הסקריפט המבוסס-API, אבל קרוב מאוד וללא עלות).

**תגלית צדדית + תיקון (5.9.2026):** טווח הימים המקורי בשתי הרוטינות (`2-6`) לא כלל יום שני (cron 0=א' → 2-6 = ג'-ש') — סגירת יום שישי שנקלטת רק ביום שני לא הייתה מקבלת כותרת אוטומטית בכלל. תוקן: הוחלף ל-`1-6` (ב'-ש') בשתי הרוטינות, אותו `RemoteTrigger update` חלקי (רק cron_expression).

## צינורות (scripts/)

| סקריפט | פלט | מקור |
|---|---|---|
| fetch_dashboards | indices.json (+aiSummary+domains) | indexes-status repo |
| fetch_momentum / fetch_ibkr | momentum.json / candidates.json | ריפו-אחים |
| fetch_gmail / fetch_barchart | briefing.json / morning.json (+notice סופ"ש) + ארכיון 30 יום | Gmail IMAP |
| fetch_market | market.json (+spark) | Yahoo v8 chart |
| fetch_econ | econ.json (צפי/בפועל) | TradingView economic calendar |
| fetch_bets | bets.json (פד, מיתון, יעד SPY; CPI כשיש נזילות) | Polymarket Gamma + Kalshi (ציבוריים, בלי מפתח) |
| fetch_earnings / fetch_news / fetch_pulse / fetch_movers | earnings/news/pulse/movers | **סורק TradingView** (CSV = גיבוי) / RSS / **טלגרם + דיג'סט X במייל** (ראו למטה) / TV scanner |
| fetch_world | world.json → טאב "שווקים בינלאומיים" | Yahoo, 26 מדדים + spark לכל שורה (ראו למטה) |
| fetch_weekend | weekend.json → רצועת "השוק סגור" בטאב שווקים | Hyperliquid info API (חוזי xyz + BTC/ETH) + Yahoo לסגירות (ראו למטה) |
| build_weekly | weekly.json + טלגרם → כרטיס "סיכום השבוע" בבית | history.json + indices.json; **אירוע, לא שעון** — רץ כשסגירת שישי נקלטת (ראו למטה) |
| fetch_sectors / fetch_trades | sectors.json / trades.json | nidam-reports |
| detect_flash | flash.json + טלגרם | ספייק 0.5%+/30 דק' (Yahoo 1m) + ראיות econ/pulse/news |
| archive_scores / archive_focus | history / focus_history | "המוקד במבחן" · archive_scores רץ תמיד *בתוך* fetch_dashboards.py (2.9.2026, כדי ש-history.json לא ייפרד מ-indices.json) — לא עוד שלב נפרד ב-update.yml |
| notify_* | טלגרם | תדרוך, תקציר, חריגים, דוחות-לילה 23:30 |

## תהליכי עבודה של איציק

שומרים ל-`C:\challenge\reports\...` — משימה מתוזמנת דוחפת כל 5 דק', האתר מושך תוך 15 דק': `sectors/`+`trades/` (HTML בשם עברי חופשי + תאריך), שורש — `TICKER__YYYY-MM-DD.html` + `earnings.csv`. בבוקר: דחיפת נתוני מדדים+מומנטום לפני 06:30.

## מלכודות שנלמדו בדם
- **לוח הדיווחים: לא לסמוך על CSV ידני** (18.9.2026). earnings.csv ב-nidam-reports היה בן 4 שבועות, 70% מהשורות בלי מועד מאושר ("--" = הערכה של Barchart) ועם מניות OTC — לנר הוצגה ב-17.9 (דיווחה 16.9), דארדן ב-17.9 (בפועל 24.9); התראת "קובץ ישן" הלכה לטלגרם הפרטי שלא מגיע. מאז `fetch_earnings.py` בונה את הלוח מ**סורק TradingView** (`load_rows_tv`: `earnings_release_date`/`_next_date` + דגל שעה 1=אחרי סגירה, ‎-1=לפני פתיחה, 0=לא ידוע; שווי מעל $1B, NASDAQ/NYSE/AMEX, stock+dr; חלון 8 ימים אחורה עד 21 קדימה; התאריך מומר לשעון ניו יורק). השורות מומרות למבנה ה-CSV כך שהיום/השבוע/window/תגובות לא השתנו. ה-CSV נשאר גיבוי בלבד (`_meta.source` אומר מי פעיל), ו-notify_earnings_age שותק כשהמקור הוא הסורק.
- **התדריך מגרוק משנה מבנה HTML בלי הודעה** (16.9.2026: תבליטי "חדש מאז" הפכו לשורות `<tr><td>` בטבלה, בלי ul/li — הבית הציג 2 מתוך 4 כי הפרסר נפל לסעיף "הידיעות המרכזיות"). `headlines_of` ב-fetch_gmail.py מנסה לפי הסדר: ul/li → תאי הטבלה הראשונה → div.bullet → פיצול •. כשמספר הידיעות בבית לא 4, לבדוק את `data/briefings/morning.html` ולהוסיף אסטרטגיה, ולבדוק רגרסיה על `data/briefings/archive/*.html`.

1. **סדר push:** commit → `git pull --rebase` → push. קונפליקט ב-data: `git checkout --theirs` (הכל רגנרטיבי). מסר-קומיט עם גרשיים ב-PowerShell נשבר — `git commit -F msgfile`.
2. **iframe דוחות בנייד:** הכיווץ ב-`__fitFrame` הוא **transform:scale על מעטפת `#np-fit-scaler`** — לא zoom! (zoom ב-WebKit משנה layout ולא מתכנס; היה חיתוך+ריצוד). origin לפי כיוון המסמך (RTL ימין / LTR שמאל); html/body מקובעים לגודל הוויזואלי; refit בפתיחת טאב (iframe שנטען בפאנל מוסתר נמדד על clientWidth=0).
3. **WebKit + גריד:** `1fr` = minmax(auto,1fr), ו-WebKit מחשב min-content של flex בלי shrink — תוכן nowrap ארוך מנפח עמודות ושובר מובייל. תרופה: `min-width:0` על פריטי גריד. כרום מסתיר את זה — בדיקות מובייל רק ב-Playwright WebKit (viewport 390; chrome headless לא יורד מ-~500 רוחב — "גלישה" בצילום 390 היא ארטיפקט).
4. **CORS של סורק TradingView:** simple request בלבד — body כ-text/plain בלי content-type.
5. **פרסרי מיילים נשברים כשהמבנה משתנה** (קורה כל כמה ימים): לעגן בכותרות עברית; פורמט 2026-08-12 = div שטוחים עם `<br>`+בולטים; סנטימנט לוכד מעבר לתגיות פנימיות (`<span class="ltr">`) ושומר לטינית/ספרות.
6. **שעון חורף (~סוף אוקטובר):** להזיז את כל ה-crons — update.yml, cron-job.org, ו-7 רוטינות הענן.
7. עריכת קבצים עם אימוג'י/עברית — Write tool, לא heredoc.
8. **שינוי ויזואלי = צילום-עין לפני push** (Playwright screenshot) — לא רק בדיקה טכנית.

## החלטות מוצר מרכזיות

- מניות במוקד = מומנטום ∩ מועמדים בלבד; הארכיון חייב להישאר זהה ל-passesBase.
- טיקר: מחיר+אחוז בלבד. חדשות באנגלית. בזק מהרשת עדיף (עדכני).
- **אופציות — "הכסף הגדול" עבר ל-SPY (12.9.2026):** איציק: "תמיד מולטי ובוליש". נבדק: ייצוא ה-SPX (350 העסקאות הגדולות) הוא 97–100% מולטי-לג כל יום, עסקאות רגל-אחת = פחות מ-1% מהפרמיה, וה-deltaTilt (Σ צד×דלתא×פרמיה) מוטה מבנית לשורי (קול עמוק בכסף = גם דלתא גבוהה וגם פרמיה ענקית) — "שורי" ב-15/19 ימים בלי קשר לשוק. ייצוא **SPY** של אותו יום: 27–72% מולטי-לג, 125–170 עסקאות רגל-אחת, ושלוש הקריאות הישרות (רגל-אחת / נפח>OI / ToOpen) מסכימות. לכן ב-indexes-status `send_report.directional_read()` קורא את הכיוון מ-`data/spy-options-flow-MM-DD-YYYY.csv` של אותו יום (נפילה ל-SPX עם `dirSource:'SPX'`), ציון ה-Flow (עוצמה) נשאר על SPX. שדות חדשים ב-daily_state.flow: `dirSource`, `dirNote`, `openTilt/openLabel/openP` (הטיית העסקאות עם נפח>OI = פוזיציות חדשות). האתר: `bigMoneyRow` מציג "חדשות: דובי" + תג SPY; `flowQuadHtml` שורת מקור. **איציק מוריד את קובץ ה-SPY יומית** (Barchart שם אותו לפי תאריך ההורדה — `normalize_incoming.py` מתקן לפי התוכן). הרוטינה היומית לא שונתה: deltaLabel נשאר השדה, רק המקור אמין יותר.
- **מומנטום → קטגוריית "מועמדים" (12.9.2026):** צ'יפ שביעי בטאב מומנטום = "מועמדות לטרייד" של דשבורד המומנטום (nditzik/stocks-momentum, `momentum_dashboard.html` → `renderTrades`): מניות שעברו את פילטר הבסיס עם **Readiness ≥ 50**, 12 המובילות. `readiness()` ב-fetch_momentum.py הוא פורט של `calcReadiness()` משם (RSI/Stoch, קרבה ל-MA50, RVOL, סיגנלים, נר ירוק) **כולל רכיב "מגמת ההיסטוריה"** (עד 10 נק'): בדשבורד הוא מחושב מסנאפשוטים ב-localStorage של הדפדפן (אחד לכל העלאת CSV); אצלנו מ-`data/_momentum_hist.json` — אותם סנאפשוטים (תאריך CSV → כל המניות שעברו בסיס + הסיגנלים שלהן), **שוחזרו לאחור מ-git של stocks-momentum** (`scripts/tools/backfill_momentum_hist.py` על clone מקומי; כל העלאה של איציק = קומיט "k"/"ל") ומתווספים ב-fetch_momentum.py בכל תאריך CSV חדש (60 ימים, כמו HISTORY_MAX_DAYS). בלי זה 2 מתוך 12 המועמדות לא תאמו לדשבורד (12.9). `passes_base()` = פורט מלא של passesBaseFilter. momentum.json שומר מעכשיו גם מניות סיגנל-אחד שהן מועמדות ועוברות בסיס, ו-`_meta.files` = שם קובץ ה-CSV של כל סורק — זה "המקור" שמוצג לכל מניה בעמודה האחרונה (צ'יפ לכל סיגנל עם שם הקובץ). **ניסיון קודם שנפסל:** קטגוריית "עסקאות" ממניות דוחות "הצעות לטרייד" — איציק: מקור לא נכון; `extract_picks()` ב-fetch_trades.py נשאר (picks ב-trades.json) אך לא מוצג. **תיקון אגב:** שורת "Downloaded from Barchart.com…" סוננה.
- **הודעות תוכן לערוץ הטלגרם — חבילה ראשונה (11.9.2026 אחה"צ, `scripts/notify_channel.py`, state `data/_channel_state.json`):** (1) **5 דקות לפתיחה** — ב'–ו' בחלון 16:05–16:29 שעון ישראל (בפועל הריצה של 16:16): חוזי S&P/Nasdaq, VIX, 10Y, המאקרו של היום (בפועל מול צפי אם כבר פורסם ב-15:30, אחרת צפי), מדווחות לפני הפתיחה/אחרי הסגירה (השדה `when` שנוסף ל-`reporting` ב-earnings.json). (2) **סיכום סגירה** — 23:05+ (הריצה של 23:16): SPY/QQQ/IWM, VIX, 10Y, 3 עולות/3 יורדות **בסריקת TradingView עם שינוי יומי** (לא movers.json — אחרי 23:00 הוא מחזיק אפטר-מרקט), מדווחות הערב. (3) **נתון מאקרו** — כשאירוע ב-econ.json מקבל `actual`: צפי מול בפועל + כיוון ההפתעה, מקובץ לפי מועד, כל אירוע פעם אחת, רק מ-36 השעות האחרונות. (4) **הניתוח היומי** — כשה-`date` של claude_analysis.json מתחלף: כותרת + tldr + שורה תחתונה + קישור `#indices`. בריצה הראשונה (3) ו-(4) רק קובעים בסיס בלי לשלוח. בדיקה: `--dry-run` מקומית, או קלט `test_channel` (preopen/close/macro/analysis) ב-workflow_dispatch — שולח **לערוץ**, עוקף שערים. חלונות הזמן בשעון ישראל דרך `il_now()` — לא תלויים ב-DST של ה-cron. **תיקון אגב:** `todayBig` ב-fetch_earnings פירש שווי גולמי של $20M כ-$20T (JVA/IHT "ענקיות") — עכשיו מספר בלי סיומת = דולרים.
- **רצועת "השוק סגור — כך נסחר סוף השבוע" (13.9.2026, רעיון של איציק):** `scripts/fetch_weekend.py` → `data/weekend.json`; `weekendStrip()` ב-app.js מתחת לכרטיסי ארה"ב בטאב שווקים, ושורת הסיכום מציגה "ארה\"ב בסוף השבוע (xyz)" במקום חוזים כשהם סגורים. **מצב weekend** = חוזי שיקגו סגורים (שישי 17:00 עד ראשון 18:00 ניו יורק): S&P 500 + NVDA, MSFT, AVGO, SNDK, GOOGL, META, AMZN, PLTR (3×3) מהחוזים התמידיים של xyz על Hyperliquid (`metaAndAssetCtxs` עם `dex:"xyz"`, ציבורי, בלי מפתח; ה-`prevDayPx` הוא 24 שעות אחורה, לא סגירת שישי — לכן הסגירה הרשמית נלקחת מנר יומי של Yahoo) + ביטקוין/את'ריום (Hyperliquid ראשי; הייחוס = המחיר בשישי 21:00 UTC מנרות 60 דק'). **מצב weekday**: הרצועה מוסתרת (עדכון אותו ערב: הקריפטו עבר לכרטיסים משלו — region "קריפטו" ב-fetch_world.py, BTC-USD/ETH-USD/XRP-USD, 24/7, מוצגים מתחת למפת העולם כמו המדדים; weekend.json עדיין מכיל btc/eth אך הרצועה לא מציגה אותם). `thin` = מחזור 24 שעות מתחת ל-$1M (אריח אפור). הנחיה מאיציק: BTC/ETH תמיד מוצגים (24/7) — ככרטיסים. זו אינדיקציה לכיוון ולריכוז, לא תחזית לפתיחה — הטקסט מתחת לרצועה אומר זאת. הרוטינות עדיין לא קוראות את הקובץ (שלב ב' אפשרי לסקירת ראשון).
- **ציון האופציות v6 (19.9.2026, אישור איציק) — מחליף את v5:** הציון בא מה**מדד** ולא ממניות: `flow_score_v6()` ב-send_report.py (indexes-status) = 60% אחוזון ההטיה משוקללת-הדלתא ב-SPY + 40% אחוזון הפוזיציות החדשות, מול 60 קבצי SPY קודמים (≥10). **ניקוי:** רק עסקאות עמוקות בכסף (|דלתא|≥0.85) — תרגיל הדיבידנד (17.9, יום לפני אקס-דיבידנד של SPY: 58% מהפרמיה, הפך "שורי" ל"מאוזן"). פקיעות של אותו יום **נשארות**: הסרתן החלישה כל קשר במחקר. **מחקר:** מעבר ראשון (40 יום, 24.7–18.9) נראה חזק: אחרי קריאה דובית S&P ‎-0.29% למחרת (עלה ב-23%), יום מכירה תוך 3 ימים 75% מול 17%. **מעבר שני (77 יום, 1.6–18.9, 20.9.2026) חתך את האפקט בחצי:** ‎-0.04% (40%) מול ‎+0.23% (60%), יום מכירה 67% מול 50% (בסיס 55%), מתאם דרגות 0.20 — ותלוי-תקופה: 0.04 ביוני–יולי, 0.44 בשמונת השבועות האחרונים. **לקח: הציון הוא תיאור של מה שהכסף הגדול עושה על המדד, לא תחזית — בלי מספרי חיזוי בטקסט שהקורא רואה** (תוקן בניתוח של 18.9; הפרומפט של הרוטינה היומית עוד מצטט 75%/17% ומשקל 25% — דורש עריכה ידנית של איציק כי יש בו מפתח). ב-SPX באותה בדיקה (98 יום): כלום. **תצוגה מול מד (רעיון של איציק):** `scores.flow` = הציון היומי (לתצוגה — הוא המנבא); `flow.meterScore` = ממוצע יומיים שנכנס ל-Combined "מאחורי הקלעים". משקלים: טכני 0.50 · אופציות **0.15** · רוחב 0.35 (היה 0.45/0.25/0.30 ביום הראשון; הורד 20.9 אחרי בדיקת 77 הימים. בדיקה חוזרת ב-~100 ימים). SPX = נורת אזהרה בלבד (`flow.spxWarning`, אחוזון מכירת-ביטוח ≥67); UOA של מניות = תצוגה בלבד (עבר לבלוק "הכסף הגדול היום"). v5 נשאר נפילה-חזרה ליום בלי קובץ SPY. **באתר:** ברייל המד — הציון היומי + "במד נכנס ממוצע יומיים"; כרטיס "ההימור נטו" = שורות SPY/SPX בלבד; **גרף חדש "ציון האופציות מול המחיר"** (`optVsPriceHtml`/`renderOptVsPrice`, מ-`flow.v6Series`; שני פאנלים על ציר זמן משותף, ריחוף מציג את היום שאחרי, והכיתוב מסכם את ההלימה מהסדרה עצמה). 18.9 חושב מחדש לפי v6 לבקשת איציק (אופציות 80→2, מד 72→60) כולל ההיסטוריות, הניתוח היומי והסיכום השבועי; קו "שינוי נוסחה v6" בציר הזמן ב-18.9. ⚠️ הפרומפט של הרוטינה היומית עדיין מתאר v5 — עדכון ממתין למשתנה הסביבה של המפתח.
- **כותרת סוף-השבוע בבית (19.9.2026, איציק):** משבת (ברגע שיש `weekly.json.narrative`) ועד **ראשון 15:00 שעון ישראל** הכותרת הראשית בבית היא סיכום השבוע — `weekendLeadHtml()` ב-app.js: H1 = המשפט הראשון של narrative.lead (בלי התאריך שבסוגריים — מתהפך ב-RTL; ארוך מ-95 תווים → עד הנקודתיים), dek = שאר ה-lead + המשפט הראשון של דוח הסקטורים, שורת "💸 לאן זרם הכסף" (רוחב סקטוריאלי שבוע-מול-שבוע בכיוון קריאה עברי "81% ← 57%", קישור לטאב סקטורים), "השבוע הבא", ומתחת הכותרת של יום המסחר האחרון. שעון ישראל דרך `Intl` (לא שעון הדפדפן). עדכון-אירוע שאינו preview ושתאריכו אחרי יום שישי (ידני/דחוף) גובר על הסיכום. מראשון 15:00 חוזרת הלוגיקה הרגילה והכותרת היא סקירת "לקראת שבוע המסחר" — לכן רוטינת nidam-preview-sun-mon רצה גם ב-14:45 (cron `45 4,11 * * 0,1`). **בלוק sectors ב-weekly.json** נבנה דטרמיניסטית ב-`build_weekly.py` (`sectors_block`/`attach_sectors`) מדוח הרוטציה של אותו שבוע (0–3 ימים אחרי יום שישי): המשפט הראשון של "בחמישה משפטים" (נפסל אם יש בו החלטות-מסחר אישיות: יצאנו/הסטופ…) + זוגות מתרשים "מכאן הכסף יצא". רץ בכל מחזור גם כשהשבוע כבר סוכם, כי הדוח מגיע בשבת בערב — אחרי הסיכום. החלטות המסחר האישיות והמכ"ם נשארים בטאב סקטורים בלבד.
- **ציון האופציות v5 (13.9.2026, אישור איציק):** מחושב ב-`flow_score_v5()` ב-send_report.py (indexes-status): 50% אחוזון נתח-הקולים בפרמיה החריגה של כל שוק המניות (UOA, מול 60 קבצים רחבים) + 20% ביטוח SPX (100 − אחוזון יחס מכירה/קנייה של פוטים) + 30% כיוון SPY (אחוזון ההטיה משוקללת-הדלתא; **נעול על 50 עד שיש 20 קבצי SPY**, 7 ב-13.9). רכיב חסר נופל והשאר מנורמלים. משקל האופציות במד קבוע 0.35 (לפני כן 0.35 × נתח כיווני ב-SPX — בימי Mid כבד ירד ל-~10%, לכן המעבר משנה את המד גם דרך המשקל: שישי 73 → 67). ציון SPX הישן נשמר כ-`flow.spxScore`. `formulaVersion` = v5 ב-daily_state/scores_history; **שורות עבר לא מחושבות מחדש** — `archive_scores` שומר `formulaVersion` ליום, ו-`renderMeterTimeline` מצייר קו מקווקו "שינוי נוסחה" ביום הראשון של גרסה חדשה (ריחוף מסביר). הציון תלוי בהורדה יומית של uoa-stocks (רחב, לא SPX-only), spy-options-flow ו-spx-options-flow. `flow.scoreNote` = הסבר עברי של ההרכב, מוצג בכרטיס "ההימור נטו".
- **שלוש שורות סנטימנט באופציות (13.9.2026, סעיף 3 של שדרוג האופציות):** `sentiment_lines()` ב-send_report.py (indexes-status) כותב `flow.research.lines` [{key, label, tone, text}] → daily_state → indices.json; `flowQuadHtml` באתר מציג אותן בראש כרטיס "ההימור נטו". (1) **מניות** — נתח הקולים מהפרמיה החריגה בייצוא UOA הכלל-שוקי (`uoa_metrics`, פרמיה ≈ Latest×Volume×100, קובץ עם ≤5 סימבולים = SPX-only ומדולג), מול ההיסטוריה של עצמו (אחוזון ב-60 קבצים רחבים; חציון ~65%). (2) **SPY** — deltaLabel + openLabel הקיימים. (3) **SPX** — put_sb (מכירה/קנייה של פוטים) עם אחוזון מול 60 קבצי SPX + IV/RV; מעל אחוזון 67 מצורף הממצא מהמחקר (מכירת ביטוח גבוהה → יום מכירה תוך 3 ימים 66% מול 31%). **תיאור בלבד, לא ציון; המד לא נוגע בזה.** כל מדד נשפט מול ההיסטוריה של המכשיר שלו, לא מול סף אוניברסלי.
- **"הכסף הגדול היום במניות" (13.9.2026):** מנוע ברפו הדשבורד — `scripts/build_big_trades.py` ב-indexes-status → `data/big_trades.json` (שלב ב-update-data.yml). קלט: הייצוא הכלל-שוקי של Barchart "Options Flow" (500 העסקאות הגדולות של היום, קובץ `options-flow-MM-DD-YYYY.csv`; הנורמלייזר מזהה אותו כ-`allflow` ומתארך לפי Exp Date−DTE, כי Barchart שם את תאריך ההורדה). ניקוי: איחוד הדפסות לפוזיציה (מניה+סוג+פקיעה, חלון 15 דק'; UNH 11.9 = 134 הדפסות באותן 10 דקות), השמטת 0DTE, ורגל עם |דלתא|≥0.85 = **תחליף מניה/ארביטראז'** (מוצג במפורש כ"לא הימור כיווני" — לא מוסתר). סיווג לפי רגל: new/roll/combo/flow; כיוון מהצד (ask/bid) כשיש ≥25% פרמיה עם צד, אחרת מסוג האופציה ומסומן "מוערך". 5 מניות לפי פרמיה כוללת, משפט עברי אחד לכל אחת. באתר: `fetch_dashboards.py` מושך את הקובץ ל-`indices.json → bigTrades`; `bigTradesHtml` ב-app.js מתחת ל"ההימור נטו" בטאב מדדים. טלגרם: הודעה חמישית בערוץ (`compose_bigmoney` ב-notify_channel.py, state `bigmoney` = תאריך הקובץ) — נשלחת פעם אחת כשתאריך הקובץ מתחלף, כי הקובץ מגיע כשאיציק מוריד אותו ולא בשעה קבועה. בדיקה: `--test bigmoney`. **זה תיאור, לא ציון** — לא נוגע במד השוק.
- **ארבע תוספות "מנתונים שכבר קיימים" (11.9.2026):** (1) **ציר הזמן של The Edge Meter** בראש טאב מדדים — `renderMeterTimeline` ב-app.js, מ-history.json; הציון על רצועות-מצב, ▲ ימי מכירה, ופס S&P **בפאנל נפרד** (לא ציר-Y כפול). המצב (30/60/90, רכיבים) ב-`MT` כי הטאב נבנה מחדש בכל רענון; `ResizeObserver` כי הטאב מוסתר בזמן הבנייה (רוחב 0). צבעי הסדרות `--mt-*` עברו את בודק-הפלטה של dataviz בשני המצבים. הספארקליין ברייל של הבית הוא קישור אליו. `archive_scores.py` שומר מעכשיו גם `headline` לכל יום (מ-claude_analysis.json כשה-date זהה; 22 ימים מולאו לאחור מ-git) — זה מה שמופיע בריחוף. (2) **"איך הגיבו המדווחות"** — טבלה בטאב **דיווחים** מתחת ללוח השבועי (`reactionsTable` ב-app.js; **הוסר מהבית** 11.9 אחה"צ לבקשת איציק — הבית מציג היום והשבוע בלבד). `reactions()` ב-fetch_earnings.py: 3 ימי-הדיווח האחרונים × 6 הגדולות, שינוי **סגירה-מול-סגירה מנרות יומיים** של Yahoo (`yahoo_bars`, range=1mo). **לא** מה-CSV: עמודות Latest/%Change שם הן מיום הייצוא השבועי, ועמודת `Time` מכילה בפועל את תאריך הייצוא; מועד הדיווח יושב ב-`Released` (Before Open/After Close). **רק מדווחות עם `Released` ידוע** — בלי מועד מדלגים (איציק, 11.9). Before Open: סגירת יום הדיווח מול היום שלפניו; After Close: סגירת יום המסחר *הבא* מול יום הדיווח. סטטוסים: `final` / `live` (סשן התגובה עדיין פתוח — מוצג "ביניים", לא כסופי) / `pending` ("מגיבה במסחר הבא"). **לקח:** הגרסה הראשונה השתמשה ב-`meta.chartPreviousClose` — זו הסגירה שלפני *תחילת הטווח* (5 ימים), לא של אתמול; מייסיז הוצגה 8.6%- במקום 4.7%-. תמיד לחשב מנרות. (3) **גרף-מיני** לכל שורה בשווקים בינלאומיים מה-`spark` שכבר נשמר; מוסתר בנייד. (4) **סיכום השבוע** — `build_weekly.py`, דטרמיניסטי (חמש הכותרות + מסלול המד + S&P שבועי + ימי מכירה), בלי LLM. טריגר: היום האחרון ב-history הוא יום שישי שטרם סוכם (`_weekly_state.json`) — כלומר נבנה ונשלח לערוץ תוך 15 דק' מדחיפת הדשבורד של סוף השבוע. **הועבר (11.9.2026 אחה"צ) לטאב מדדים, מתחת לציר הזמן (`#weekly-slot`, `renderWeekly`), ומוצג כל השבוע** — תמיד השבוע השלם האחרון, מתחלף כשהבא נבנה. מתחת לאריחים: **סיכום מילולי** (`narrative`: lead/news/earnings/macro/lookahead/writtenAt) שרוטינת `nidam-weekly-narrative` כותבת בסופ"ש; `build_weekly.py` משמר narrative קיים בבנייה מחדש של אותו שבוע. `--force` לבדיקה מקומית.
- **שווקים בינלאומיים (9.9.2026):** 25 מדדים ב-5 קבוצות (ת"א SME60 הוסר 11.9 לבקשת איציק) (ישראל-ראשיים / ישראל-סקטורים / אסיה / אירופה / ארה"ב), `fetch_world.py` על מחזור ה-15 דק'. **כל שורה נושאת מצב מסחר ולא רק מחיר** — הבורסות פרוסות על פני העולם ולכן רובן סגורות רוב הזמן, ו"DAX 0.00%" בבוקר הוא נעילת אתמול. המצב נגזר מחותמת הזמן של הציטוט ולא מהנחת לוח-שעות. **מצב `stale`** (>5 ימים) נבדק לפני כל השאר, אחרי שהתגלה שת"א-קנאביס (186.TA) מוקפא מ-2022 עם מחיר תקין-למראה; השנה מוצגת אוטומטית כשהציטוט אינו מהשנה הנוכחית, כי `DD/MM` בלי שנה גרם לקרוא ציטוט בן 4 שנים כטרי. **סמלים שאסור "לתקן" להיגיון** — אותרו רק בסריקה בפועל: `^TA125.TA` (גרש+סיומת), `TA-BANKS.TA`, `207.TA`=ביטחוניות, `209.TA`=טכנולוגיה, `56.TA`=נדל"ן מניב, `55.TA`=בנייה, `330.TA`=ביטוח, `000001.SS`=שנגחאי. **דגלים:** ווינדוס לא כולל גופן דגלים ומרנדר 🇮🇱 כ-"IL" — נפתר עם Noto Color Emoji מגוגל, מצומצם ב-`text=` לדגלים שבשימוש (להרחיב שם בהוספת מדינה). תל בונד (17 מדדי אג"ח קונצרני) נבדק ונדחה — סוג נכס אחר. **עיצוב מחדש (11.9.2026 אחה"צ, "כל נתון על שורה זה בזבוז"):** `renderWorld` = (1) שורת סיכום עולמי — ממוצע שינוי לאזור, ארה"ב מהחוזים (TICKD) כשהשוק סגור; (2) שעון עולמי — צ'יפים לפי שעת פתיחה (השדה `hours` שנוסף ל-world.json), פתוח מואר; (3) שתי עמודות במחשב: ישראל (4 כרטיסי `.wc` + **מפת חום** `.wh` של 11 הסקטורים, ממוינת, צבע = עוצמת השינוי) מול העולם (כרטיסים לפי אזור); בנייד הכרטיסים רצועת גלילה אופקית. הכרטיס: מחיר, שינוי, גרף-מיני, **5 ימים ומתחילת השנה** (`chg5d`/`chgYtd` מ-`daily()` — range=ytd, ה-chartPreviousClose שם = סגירת השנה הקודמת). **תיקון 0.00% ביום ללא מסחר:** כשהשוק סגור השינוי הוא מהנרות היומיים (נר תאריך-הציטוט מול קודמו), כי range=1d ביום ריק מחזיר prev=price. **לסקטורי ת"א אין היסטוריה יומית ב-Yahoo** — נבנית מנרות 15 דק' של חודש (סגירה אחרונה לכל תאריך); לכן אין להם YTD. קווי הפרדה: אנכי בין עמודת ישראל לעולם, אופקי בין אזורים (`.wm-col + .wm-col`, `.wm-grp + .wm-grp`). **מפת העולם (11.9 ערב):** מתחת לסקטורי ישראל, `renderWorldMap` ב-app.js; הגבולות ב-`assets/worldmap.json` (74KB, נבנה פעם אחת ע"י `scripts/tools/build_worldmap.py` מ-world-atlas 110m, היטל שטוח 1000×440, אנטארקטיקה מושמטת, טבעות שחוצות את קו התאריך נשברות — אחרת קיריבטי מציירת קו לרוחב המפה). בזמן ריצה: מדינה נצבעת לפי `chg` של המדד המייצג (`WMAP_COUNTRY`: ISO-numeric → key; ארה"ב=S&P, ישראל=ת"א 125), נקודה לכל בורסה (`WMAP_CITIES`, פועמת כשנסחר, צהובה לפני פתיחה), וקו יום/לילה מהשעה (נטיית השמש + משוואת הזמן בקירוב). לחיצה על מדינה/נקודה גוללת לכרטיס (`#wc-<key>`). בלי ספרייה.
- **בזק מהרשת — שני צינורות מכוונים (5.9.2026; X Scan הוחלף 21.9.2026):** (1) 4 שיקופי טלגרם, חינם; (2) "X Scan" במייל — בוט (x.ai) של איציק סורק ~8 חשבונות בערך כל 3.5 שעות ושולח HTML בעברית, נושא "X Scan HH:MM – DD.MM.YYYY", מקובץ לפי חשבון (`<h2>@handle</h2><ul><li>HH:MM — טקסט</li>…</ul>`), נקלט ב-IMAP (סודות `GMAIL_*`, **חייבים להיות על שלב `Fetch X pulse` ב-update.yml** — בלעדיהם דילוג שקט). החפיפה בין הצינורות מכוונת כרזרביות; נפילת אחד לא מרוקנת את הרצועה. `parse_xscan()`/`fetch_xscan()` ב-`fetch_pulse.py`: `XSCAN_BLOCK_RE`/`XSCAN_LI_RE` (regex, לא פרסר HTML), התאריך של כל פריט נגזר מרגע-השליחה עם גלגול-יום לחלון שחוצה חצות (23:30–07:00), אין קישור לכל פריט אז ברירת המחדל היא פרופיל החשבון. `XD_ALIASES` ממפה @DeItaone→Walter Bloomberg ו-@FinancialJuice→FinancialJuice, אחרת תקרת MAX_PER_SOURCE נפתחת ל-6. סורק כמה מיילי X Scan אחרונים (לא רק האחרון) כי כל אחד "רק חדש מאז הקודם" ומכסה חלון חלקי בלבד. **גרסה קודמת (5.9–21.9.2026, מעולם לא הגיעה בפועל בפורמט הזה):** תוכנן לפורמט `תאריך ||| שעה ||| @handle ||| אנגלית ||| עברית ||| קישור` בנושא `X-PULSE` — איציק בנה אוטומציה אחרת בפועל; `parse_xdigest`/`fetch_xdigest` (וה-upgrade-לתרגום שתלוי ב-`_key`) הוחלפו/הפכו ל-no-op. **פתוח:** אין התראה כשה-X Scan מפסיק להגיע.
- הימורים: הסתברות + שינוי יומי בנקודות; התפלגות מלאה לפד-ספטמבר; אירועי-סיכון נקודתיים נדחו במכוון.
- מבזק ⚡: מציג "מה שהתפרסם באותן דקות" — לא טוען סיבתיות. סף 0.5%/30 דק' (בניסיון כיול).
- GoatCounter — אנליטיקס; מעברי-טאב נספרים כצפיות.

## עיצוב הכותרת בבית (20.9.2026, np68)
שלד אחד לסוף שבוע וליום חול ("פחות מילים יותר נתונים", אושר במוקאפ): קיקר · H1 · `leadStats` (3 נתונים) · טקסט קצר · אריחים · נוריות.
- **סוף שבוע** (`weekendLeadHtml`): S&P שבועי / VIX תחילה→סוף / % מניות במגמת עלייה (`sectors.marketBreadth`); dek = משפט ראשון מהסיכום + משפט ראשון מדוח הסקטורים; `sectorTilesHtml` (פס אפור = לפני שבוע, צבעוני = עכשיו).
- **יום חול**: S&P יומי / המניה הממוצעת (regex על "שוויוני" בטקסט מנוע המסקנות; נפילה ל-%MA200) / VIX; בבית רק **המשפט הראשון של tldr** (הרוטינה לא השתנתה); `bigMoneyTilesHtml` = 5 הפוזיציות (4 בטלפון), רק כש-`bigTrades.date === date`.
- **רייל**: `leadAgendaHtml` מתחת למד — שבת/ראשון "🔭 השבוע הבא" (EARN.upcoming + econ), יום חול "📅 היום ביומן" (reporting לפי `when`, מאקרו של היום עם צפי/בפועל, הבא בתור). מתרענן כש-EARN/ECON נטענים (`refreshLeadAgenda`). ה-lookahead המילולי מוצג רק כנפילה כשאין נתוני יומן.
- מצב עדכון-אירוע (eventUpdate) לא שונה.

## טאב Insider (20.9.2026, np69)
דוחות "קניות של בעלי עניין". איציק שומר HTML ל-`C:\challenge\reports\insider` → הסנכרון האוטומטי דוחף ל-nidam-reports/insider → `scripts/fetch_insider.py` (צעד ב-update.yml אחרי trades) → `data/insider.json` + `data/insider/insider-<iso>.html` → `renderInsider`/`showInsider` (כמו סקטורים: אחרון מוצג, קודמים בצ'יפים).
- תאריך: משם הקובץ (YYYY-MM-DD / D.M.YYYY) → "הופק ב-D.M.YYYY" בתוך הדוח → YYYY-MM בשם (ה-1 בחודש). השם הנוכחי חודשי (`insider-buying-report-2026-09.html`).
- **זיכרון:** הסקריפט ממזג את הרשימה הקודמת — דוח שנדרס/נמחק במקור נשאר באתר כל עוד העתק ה-HTML קיים. (שונה מ-sectors/trades שנבנים רק מהמקור.)
- נשלפים גם `range` ("עסקאות מ-…") ו-`tickers` (השורות הממוספרות בטבלת הדירוג) → צ'יפים + חיפוש.

## הצפי השבועי (20.9.2026, np73)
איציק: "צפי אמיתי לשבוע, אפשר לקחת סיכון גם אם לא נפגע במאה אחוז". ארבעה חלקים:
1. `scripts/build_week_ahead.py` → `data/week_ahead.json` (שבת/ראשון; פעם ביום + פעם אחרי 14:00): עונתיות של **אותו שבוע ביחס לפקיעה החודשית** מאז 1990 (Yahoo ^GSPC), מצב המד/רוחב/אופציות/סקטורים, KNN + הרקורד שלו (מדשבורד המדדים), יומן, וסוף השבוע: pulse/news/תדרוכים + `web` = Google News RSS לפי נושא (טראמפ, AI, פד, סין, גיאופוליטיקה, נפט, ענקיות, week ahead, משפיעים). **לרוטינה אין רשת — הבוט סורק בשבילה.**
2. `scripts/prompts/week_forecast.md` — הוראות הצפי. הרוטינה `nidam-preview-sun-mon` קוראת אותו (שורה אחת בפרומפט שאיציק הדביק); שינויים בהוראות נעשים בריפו, בלי לגעת בפרומפט.
3. `eventUpdate.forecast` (weekOf, direction, label, prob ≥50, claim, ref, test, rangeLow/High, summary, bear[], bull[], pivot, invalidation) → `forecastHtml` בכותרת "לקראת שבוע המסחר". הרוטינה של שני 07:45 דורסת את eventUpdate → האתר נופל ל-`forecasts.json.current`.
4. `scripts/score_forecast.py` → `data/forecasts.json`: מארכב, **נועל בפתיחת שני 16:30**, ומציין בשבת מול סגירת השבוע ב-weekly.json (hit + inRange). המאזן מוצג בבלוק הצפי.
הצפי הראשון (21–25.9) נכתב ידנית בסשן: "שבוע מפוצל, נטייה קלה למטה", 60% לסגירה מתחת ל-7,650.
באותו יום ירדה מהכותרת שורת המכוונים (מניות/סקטורים/אופציות + 4 נוריות) — נשאר רק "הניתוח המלא ←"; וכותרות רגועות (preview/preopen/midday) קיבלו `marketStats` + משפט ראשון בלבד.

## טאב Barchart (21.9.2026, np75, בקשת איציק)
הטאב שהיה "סקירת בוקר" הפך ל-Barchart עם שתי מהדורות מאותו שולח (nditzik@gmail.com):
- **review** (06:00): "דוח Barchart יומי" — כמו קודם, בשורש data/morning.json (תאימות-לאחור, כי הרוטינות קוראות d.subject/d.file ישירות).
- **premkt** (14:00): "טרום מסחר בוול סטריט" — מקונן תחת morning.json.premkt. `fetch_barchart.py` סורק את שתי המהדורות באותה ריצת IMAP (PREMKT_MARK = "טרום מסחר בוול סטריט", כותרת דינמית, בלי קידומת קבועה ארוכה), מארכב את שתיהן דרך briefing_archive.py (kind="review"/"premkt"), מהדורה חסרה לא דורסת את השנייה.
- `renderMorning` ב-app.js (כמו renderBriefing לתדרוך): טאב-צ'יפים בין המהדורות (premkt ברירת מחדל, כי הוא מאוחר יותר ביום), + צ'יפי היסטוריה לפי יום (archDays(["review","premkt"])).
- אין עדיין תצפית על יציבות הקצב של premkt (רק דגימה אחת ב-21.9) — לעקוב אחרי כמה ימי מסחר שהוא באמת מגיע כל יום ~14:00.
